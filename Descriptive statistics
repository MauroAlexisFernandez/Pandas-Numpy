import pandas as pd

df = pd.read_csv("cars_depurado.csv")

df

df.dtypes

df["price_usd"].mean()

df["price_usd"].median()

df["price_usd"].plot.hist(bins=10)

print(f"Most expensive: ${ df['price_usd'].max() } ")

print(f"Least expensive: ${ df['price_usd'].min() } ")

